{
  "name": "cmass-sales-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Lightweight test harness for input meta->DOM",
  "scripts": {
    "test": "mocha \"tests/**/*.test.js\" --timeout 5000"
  },
  "dependencies": {
    "googleapis": "^121.0.0",
    "playwright": "^1.56.1",
    "puppeteer": "^24.26.1"
  },
  "devDependencies": {
    "jsdom": "^22.1.0",
    "mocha": "^10.2.0"
  }
}
